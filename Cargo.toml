[package]
name = "rustchain-contributor-ladder"
version = "0.1.0"
edition = "2021"
description = "RustChain Contributor Ladder - Tier management and rewards"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }