---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart rustchain
  ansible.builtin.systemd:
    name: rustchain-node
    state: restarted

- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
